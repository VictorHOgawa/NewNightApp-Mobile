import { styled } from "styled-components/native";
import Theme from "../../styles/themes";

export const Error = styled.Text`
  font-size: 1rem;
  font-weight: bold;
  color: ${Theme.color.red_60};
`;
